<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="yoyo.collacoken.org.test">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gogreen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gogreen"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">


        </activity>
        <activity android:name=".progress" />
        <activity android:name=".wallet" />
        <activity android:name=".camera" />
        <activity android:name=".leaderboardcardmodel" />
        <activity android:name=".leaderboard" />
        <activity android:name=".Products" />
        <activity android:name=".Events" />
        <activity android:name=".signup_activity"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar">


        </activity>
    </application>

    <dist:module dist:instant="true" />

</manifest>